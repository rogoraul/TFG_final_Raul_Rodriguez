[pytest]
testpaths = tests
norecursedirs =
    .git
    .pytest_cache
    __pycache__
    backtests/.cache
    copia
markers =
    integration_sql: requires a local SQL database and is skipped by default unless RUN_REAL_SQL_TESTS=1
