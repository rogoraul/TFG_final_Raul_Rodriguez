"""Dashboard support modules for Trading Center.

The public entrypoint remains ``trading_center.dash_readonly_app``.  Modules in
this package host extracted helpers so the entrypoint can shrink without
breaking existing imports, docs, or CLI usage.
"""
